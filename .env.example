# Plex claim token (get from https://www.plex.tv/claim/ - valid for 4 minutes)
PLEX_CLAIM="claim-xxxxxxxxxxxx"

# User/Group IDs
# Linux (Ubuntu/Debian): Usually 1000/1000 (default if not set)
# macOS: Usually 501/20
# Find yours with: id -u (PUID) and id -g (PGID)
PUID=1000
PGID=1000

# Media directories
# Point these to your actual media locations
MOVIES_DIR=/path/to/your/movies
TV_DIR=/path/to/your/tv
